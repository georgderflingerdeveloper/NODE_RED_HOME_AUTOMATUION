

c = Bus.control(s, 1);
n = NetAddr("127.0.0.1", 3000);
(
SynthDef(\busCopy, {|bus|
	var x = SoundIn.ar(1);
	Out.kr(bus, Pitch.kr(x,minFreq:20));
}).add;
)
(
r = Routine({
	inf.do({
		var val = c.getSynchronous;
		n.sendMsg(\pitch, cpsmidi(val));
		1.wait;
	});
});
)


b = Synth(\busCopy, [\bus, c]);
r.play;